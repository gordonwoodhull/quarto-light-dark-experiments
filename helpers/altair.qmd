---
title: "jupyter dark mode - altair"
engine: jupyter
brand:
  light: ../united-brand.yml
  dark: ../slate-brand.yml
keep-md: true
---


```{python}
from quarto import altair_theme_brand
import altair as alt

united_theme = altair_theme_brand('../united-brand.yml')
slate_theme = altair_theme_brand('../slate-brand.yml')

alt.theme.register('united_theme', enable=False)(united_theme)
alt.theme.register('slate_theme', enable=False)(slate_theme);
```

```{python}
#| echo: false

# load a sample dataset as a pandas DataFrame
from vega_datasets import data
cars = data.cars()
_ = alt.renderers.enable('html')
```

### No crossref or caption

```{python}
#| echo: false
#| renderings: [light, dark]

chart = alt.Chart(cars).mark_point().encode(
        x='Horsepower',
        y='Miles_per_Gallon',
        color='Origin',
    ).properties(width=800, height=500).interactive()

alt.theme.enable('united_theme')
chart.show()

alt.theme.enable('slate_theme')
chart.show()
```


### with crossref but no caption

::: {#fig-altair-hp-year}

```{python}
#| echo: false
#| renderings: [light, dark]

chart = alt.Chart(cars).mark_point().encode(
        x='Year',
        y='Horsepower',
        color='Origin',
    ).properties(width=800, height=500).interactive()

alt.theme.enable('united_theme')
chart.show()

alt.theme.enable('slate_theme')
chart.show()
```

:::

### caption but no crossref

::: {}

```{python}
#| echo: false
#| renderings: [light, dark]

chart = alt.Chart(cars).mark_point().encode(
        x='Horsepower',
        y='Acceleration',
        color='Origin',
    ).properties(width=800, height=500).interactive()

alt.theme.enable('united_theme')
chart.show()

alt.theme.enable('slate_theme')
chart.show()
```

cars dataset, acceleration by horsepower
:::

### with crossref and caption


::: {#fig-altair-cars-acc-year}

```{python}
#| echo: false
#| renderings: [light, dark]

chart = alt.Chart(cars).mark_point().encode(
        x='Year',
        y='Acceleration',
        color='Origin',
    ).properties(width=800, height=500).interactive()

alt.theme.enable('united_theme')
chart.show()

alt.theme.enable('slate_theme')
chart.show()
```

cars dataset, acceleration by year
:::

{{< lipsum 2 >}}